/*
 * Copyright 2026 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *     https://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
// Generated by the protocol buffer compiler.  DO NOT EDIT!
// NO CHECKED-IN PROTOBUF GENCODE
// source: google/cloud/apptopology/v1/query.proto
// Protobuf Java Version: 4.33.6

package com.google.cloud.apptopology.v1;

/**
 *
 *
 * <pre>
 * Matcher for label and property. The matcher accepts both exact match and
 * expression.
 * </pre>
 *
 * Protobuf type {@code google.cloud.apptopology.v1.LabelPropertiesPattern}
 */
@com.google.protobuf.Generated
public final class LabelPropertiesPattern extends com.google.protobuf.GeneratedMessage
    implements
    // @@protoc_insertion_point(message_implements:google.cloud.apptopology.v1.LabelPropertiesPattern)
    LabelPropertiesPatternOrBuilder {
  private static final long serialVersionUID = 0L;

  static {
    com.google.protobuf.RuntimeVersion.validateProtobufGencodeVersion(
        com.google.protobuf.RuntimeVersion.RuntimeDomain.PUBLIC,
        /* major= */ 4,
        /* minor= */ 33,
        /* patch= */ 6,
        /* suffix= */ "",
        "LabelPropertiesPattern");
  }

  // Use LabelPropertiesPattern.newBuilder() to construct.
  private LabelPropertiesPattern(com.google.protobuf.GeneratedMessage.Builder<?> builder) {
    super(builder);
  }

  private LabelPropertiesPattern() {
    labelMatcherExpr_ = "";
    propertyMatcherExpr_ = "";
  }

  public static final com.google.protobuf.Descriptors.Descriptor getDescriptor() {
    return com.google.cloud.apptopology.v1.QueryProto
        .internal_static_google_cloud_apptopology_v1_LabelPropertiesPattern_descriptor;
  }

  @java.lang.Override
  protected com.google.protobuf.GeneratedMessage.FieldAccessorTable
      internalGetFieldAccessorTable() {
    return com.google.cloud.apptopology.v1.QueryProto
        .internal_static_google_cloud_apptopology_v1_LabelPropertiesPattern_fieldAccessorTable
        .ensureFieldAccessorsInitialized(
            com.google.cloud.apptopology.v1.LabelPropertiesPattern.class,
            com.google.cloud.apptopology.v1.LabelPropertiesPattern.Builder.class);
  }

  public static final int LABEL_MATCHER_EXPR_FIELD_NUMBER = 1;

  @SuppressWarnings("serial")
  private volatile java.lang.Object labelMatcherExpr_ = "";

  /**
   *
   *
   * <pre>
   * Required. Matcher expression for labels. Allow OR / AND operators;
   * The syntax is defined by the following grammar:
   * - The label expression can use either `OR` or `AND` operator, but not a
   * combination of both.
   * Example: `AppHub/Application`, `AppHub/Workload OR AppHub/Service`
   * </pre>
   *
   * <code>string label_matcher_expr = 1 [(.google.api.field_behavior) = REQUIRED];</code>
   *
   * @return The labelMatcherExpr.
   */
  @java.lang.Override
  public java.lang.String getLabelMatcherExpr() {
    java.lang.Object ref = labelMatcherExpr_;
    if (ref instanceof java.lang.String) {
      return (java.lang.String) ref;
    } else {
      com.google.protobuf.ByteString bs = (com.google.protobuf.ByteString) ref;
      java.lang.String s = bs.toStringUtf8();
      labelMatcherExpr_ = s;
      return s;
    }
  }

  /**
   *
   *
   * <pre>
   * Required. Matcher expression for labels. Allow OR / AND operators;
   * The syntax is defined by the following grammar:
   * - The label expression can use either `OR` or `AND` operator, but not a
   * combination of both.
   * Example: `AppHub/Application`, `AppHub/Workload OR AppHub/Service`
   * </pre>
   *
   * <code>string label_matcher_expr = 1 [(.google.api.field_behavior) = REQUIRED];</code>
   *
   * @return The bytes for labelMatcherExpr.
   */
  @java.lang.Override
  public com.google.protobuf.ByteString getLabelMatcherExprBytes() {
    java.lang.Object ref = labelMatcherExpr_;
    if (ref instanceof java.lang.String) {
      com.google.protobuf.ByteString b =
          com.google.protobuf.ByteString.copyFromUtf8((java.lang.String) ref);
      labelMatcherExpr_ = b;
      return b;
    } else {
      return (com.google.protobuf.ByteString) ref;
    }
  }

  public static final int PROPERTY_MATCHER_EXPR_FIELD_NUMBER = 2;

  @SuppressWarnings("serial")
  private volatile java.lang.Object propertyMatcherExpr_ = "";

  /**
   *
   *
   * <pre>
   * Optional. The syntax of the filter is defined by the following grammar:
   * https://google.aip.dev/160.
   *
   * Referencing properties of graph elements using traversal operator `.`.
   * Repeated properties cannot be traversed.
   * e.g. `e.NodeName`, where `e` is a node or edge alias.
   *
   * The following properties are supported for all node and edge types:
   *
   * - `Labels`: repeated string property. Can be used to combine conditions
   * between labels and properties.
   * e.g. `e.Labels : "label_1"` to match a node/edge with label
   * `label_1`.
   * - `NodeName/EdgeName`: string property. Support for filtering by node or
   * edge names.
   * e.g. `app.NodeName = "apphub.io/Application"`
   *
   * Other properties types can be found at
   * https://apptopology.googleapis.com/v1/GetSchema
   *
   * The following operators are supported on properties:
   * 1. Checking if a non-repeated property of a graph element is equal to a
   * scalar value using the equality operator `=`.
   * - LHS must be a node/edge property.
   * - RHS must be a scalar literal.
   * - Supported value types: string (enclosed in double quotes), int, double,
   * and boolean.
   * e.g. `e.project = "project_1"`
   *
   * 2. Checking if a non-repeated property of a graph element satisfies a
   * condition using the comparison operators `&gt;`, `&lt;`, `&gt;=`, `&lt;=`.
   * - LHS must be a node/edge property.
   * - RHS must be a scalar literal.
   * - Supported value types: string (enclosed in double quotes), int, double.
   * e.g. `e.NumericProperty &lt; 0.1`
   *
   * 3. Checking if a scalar value is present in a repeated property
   * HAS operator `:`.
   * - LHS must be a repeated property.
   * - RHS must be a scalar literal.
   * - Supported value types: string (enclosed in double quotes), int, double.
   * e.g. `e.Labels : "label_1"`, where `e.Labels` is a repeated string
   * property.
   *
   * 4. Checking if a property is present using HAS_PROPERTY operator `:`.
   * - LHS must be a node or edge alias.
   * - RHS must be a property name.
   * e.g. `e : Base/Location`, where `e` is a node or edge alias.
   *
   * 5. Combining multiple filters using logical operators : `AND`, and `OR`.
   * - Parentheses must be used to resolve ambiguity.
   * e.g. `e.Base/Location = "us-central1" AND e.project = "project_1"`
   *
   * 6. The following custom functions are supported:
   * - **EQUALS_ANY(arg1, arg2, arg3, ...)**
   * * Checks if `arg1` (a non-repeated property) equals any of the
   * subsequent scalar literal arguments.
   * * arg1: non-repeated property. Supported value types: string
   * (enclosed in double quotes), int, double.
   * * arg2, arg3, ...: The remaining arguments are literal values to
   * compare against. All the arguments must be of the same type.
   * * e.g. `EQUALS_ANY(e.project, "project_1", "project_2")`
   *
   * - **CONTAINS_ANY(arg1, arg2, arg3, ...)**
   * * Checks if a repeated property has any of the given scalar literals.
   * arg1: repeated_property. Supported element types: string
   * (enclosed in double quotes), int, double.
   * * arg2, arg3, ...: The remaining arguments are literal values to
   * compare against. All the arguments must be of the same type.
   * * e.g. `CONTAINS_ANY(e.folders, "folder_1", "folder_2")`
   *
   * - **CONTAINS_ANY_LESS_THAN(arg1, arg2)**
   * * Checks if a repeated property has any element less than the given
   * scalar literal.
   * * arg1: repeated_property. Supported element types are: int,
   * double.
   * * arg2: scalar literal
   * * e.g. `CONTAINS_ANY_LESS_THAN(e.NumericProperty, 0.1)`
   *
   * - **CONTAINS_ANY_GREATER_THAN(arg1, arg2)**
   * * Checks if a repeated property has any element greater than the given
   * scalar literal.
   * * arg1: repeated_property. Supported element types are: int,
   * double.
   * * arg2: scalar literal
   * * e.g. `CONTAINS_ANY_GREATER_THAN(e.NumericProperty, 0.1)`
   *
   * - **CONTAINS_ANY_LESS_OR_EQUAL(arg1, arg2)**
   * * Checks if a repeated property has any element less than or equal to
   * the given scalar literal.
   * * arg1: repeated_property. Supported element types are: int,
   * double.
   * * arg2: scalar literal
   * * e.g. `CONTAINS_ANY_LESS_OR_EQUAL(e.NumericProperty, 0.1)`
   *
   * - **CONTAINS_ANY_GREATER_OR_EQUAL(arg1, arg2)**
   * * Checks if a repeated property has any element greater than or equal
   * to the given scalar literal.
   * * arg1: repeated_property. Supported element types are: int,
   * double.
   * * arg2: scalar literal
   * * e.g. `CONTAINS_ANY_GREATER_OR_EQUAL(e.NumericProperty, 0.1)`
   *
   * NOTE:
   * - For Alert nodes, only the equality operator `=` is supported.
   * The expression cannot be a composite boolean expression (i.e., combining
   * multiple conditions with AND/OR is not supported; it must be simple).
   * e.g. `a.NodeName = "alert-1"` is supported, but
   * `a.NodeName = "alert-1" OR a.NodeName = "alert-2"` is not supported.
   * - For traffic edges, filters cannot be added.
   * </pre>
   *
   * <code>string property_matcher_expr = 2 [(.google.api.field_behavior) = OPTIONAL];</code>
   *
   * @return The propertyMatcherExpr.
   */
  @java.lang.Override
  public java.lang.String getPropertyMatcherExpr() {
    java.lang.Object ref = propertyMatcherExpr_;
    if (ref instanceof java.lang.String) {
      return (java.lang.String) ref;
    } else {
      com.google.protobuf.ByteString bs = (com.google.protobuf.ByteString) ref;
      java.lang.String s = bs.toStringUtf8();
      propertyMatcherExpr_ = s;
      return s;
    }
  }

  /**
   *
   *
   * <pre>
   * Optional. The syntax of the filter is defined by the following grammar:
   * https://google.aip.dev/160.
   *
   * Referencing properties of graph elements using traversal operator `.`.
   * Repeated properties cannot be traversed.
   * e.g. `e.NodeName`, where `e` is a node or edge alias.
   *
   * The following properties are supported for all node and edge types:
   *
   * - `Labels`: repeated string property. Can be used to combine conditions
   * between labels and properties.
   * e.g. `e.Labels : "label_1"` to match a node/edge with label
   * `label_1`.
   * - `NodeName/EdgeName`: string property. Support for filtering by node or
   * edge names.
   * e.g. `app.NodeName = "apphub.io/Application"`
   *
   * Other properties types can be found at
   * https://apptopology.googleapis.com/v1/GetSchema
   *
   * The following operators are supported on properties:
   * 1. Checking if a non-repeated property of a graph element is equal to a
   * scalar value using the equality operator `=`.
   * - LHS must be a node/edge property.
   * - RHS must be a scalar literal.
   * - Supported value types: string (enclosed in double quotes), int, double,
   * and boolean.
   * e.g. `e.project = "project_1"`
   *
   * 2. Checking if a non-repeated property of a graph element satisfies a
   * condition using the comparison operators `&gt;`, `&lt;`, `&gt;=`, `&lt;=`.
   * - LHS must be a node/edge property.
   * - RHS must be a scalar literal.
   * - Supported value types: string (enclosed in double quotes), int, double.
   * e.g. `e.NumericProperty &lt; 0.1`
   *
   * 3. Checking if a scalar value is present in a repeated property
   * HAS operator `:`.
   * - LHS must be a repeated property.
   * - RHS must be a scalar literal.
   * - Supported value types: string (enclosed in double quotes), int, double.
   * e.g. `e.Labels : "label_1"`, where `e.Labels` is a repeated string
   * property.
   *
   * 4. Checking if a property is present using HAS_PROPERTY operator `:`.
   * - LHS must be a node or edge alias.
   * - RHS must be a property name.
   * e.g. `e : Base/Location`, where `e` is a node or edge alias.
   *
   * 5. Combining multiple filters using logical operators : `AND`, and `OR`.
   * - Parentheses must be used to resolve ambiguity.
   * e.g. `e.Base/Location = "us-central1" AND e.project = "project_1"`
   *
   * 6. The following custom functions are supported:
   * - **EQUALS_ANY(arg1, arg2, arg3, ...)**
   * * Checks if `arg1` (a non-repeated property) equals any of the
   * subsequent scalar literal arguments.
   * * arg1: non-repeated property. Supported value types: string
   * (enclosed in double quotes), int, double.
   * * arg2, arg3, ...: The remaining arguments are literal values to
   * compare against. All the arguments must be of the same type.
   * * e.g. `EQUALS_ANY(e.project, "project_1", "project_2")`
   *
   * - **CONTAINS_ANY(arg1, arg2, arg3, ...)**
   * * Checks if a repeated property has any of the given scalar literals.
   * arg1: repeated_property. Supported element types: string
   * (enclosed in double quotes), int, double.
   * * arg2, arg3, ...: The remaining arguments are literal values to
   * compare against. All the arguments must be of the same type.
   * * e.g. `CONTAINS_ANY(e.folders, "folder_1", "folder_2")`
   *
   * - **CONTAINS_ANY_LESS_THAN(arg1, arg2)**
   * * Checks if a repeated property has any element less than the given
   * scalar literal.
   * * arg1: repeated_property. Supported element types are: int,
   * double.
   * * arg2: scalar literal
   * * e.g. `CONTAINS_ANY_LESS_THAN(e.NumericProperty, 0.1)`
   *
   * - **CONTAINS_ANY_GREATER_THAN(arg1, arg2)**
   * * Checks if a repeated property has any element greater than the given
   * scalar literal.
   * * arg1: repeated_property. Supported element types are: int,
   * double.
   * * arg2: scalar literal
   * * e.g. `CONTAINS_ANY_GREATER_THAN(e.NumericProperty, 0.1)`
   *
   * - **CONTAINS_ANY_LESS_OR_EQUAL(arg1, arg2)**
   * * Checks if a repeated property has any element less than or equal to
   * the given scalar literal.
   * * arg1: repeated_property. Supported element types are: int,
   * double.
   * * arg2: scalar literal
   * * e.g. `CONTAINS_ANY_LESS_OR_EQUAL(e.NumericProperty, 0.1)`
   *
   * - **CONTAINS_ANY_GREATER_OR_EQUAL(arg1, arg2)**
   * * Checks if a repeated property has any element greater than or equal
   * to the given scalar literal.
   * * arg1: repeated_property. Supported element types are: int,
   * double.
   * * arg2: scalar literal
   * * e.g. `CONTAINS_ANY_GREATER_OR_EQUAL(e.NumericProperty, 0.1)`
   *
   * NOTE:
   * - For Alert nodes, only the equality operator `=` is supported.
   * The expression cannot be a composite boolean expression (i.e., combining
   * multiple conditions with AND/OR is not supported; it must be simple).
   * e.g. `a.NodeName = "alert-1"` is supported, but
   * `a.NodeName = "alert-1" OR a.NodeName = "alert-2"` is not supported.
   * - For traffic edges, filters cannot be added.
   * </pre>
   *
   * <code>string property_matcher_expr = 2 [(.google.api.field_behavior) = OPTIONAL];</code>
   *
   * @return The bytes for propertyMatcherExpr.
   */
  @java.lang.Override
  public com.google.protobuf.ByteString getPropertyMatcherExprBytes() {
    java.lang.Object ref = propertyMatcherExpr_;
    if (ref instanceof java.lang.String) {
      com.google.protobuf.ByteString b =
          com.google.protobuf.ByteString.copyFromUtf8((java.lang.String) ref);
      propertyMatcherExpr_ = b;
      return b;
    } else {
      return (com.google.protobuf.ByteString) ref;
    }
  }

  private byte memoizedIsInitialized = -1;

  @java.lang.Override
  public final boolean isInitialized() {
    byte isInitialized = memoizedIsInitialized;
    if (isInitialized == 1) return true;
    if (isInitialized == 0) return false;

    memoizedIsInitialized = 1;
    return true;
  }

  @java.lang.Override
  public void writeTo(com.google.protobuf.CodedOutputStream output) throws java.io.IOException {
    if (!com.google.protobuf.GeneratedMessage.isStringEmpty(labelMatcherExpr_)) {
      com.google.protobuf.GeneratedMessage.writeString(output, 1, labelMatcherExpr_);
    }
    if (!com.google.protobuf.GeneratedMessage.isStringEmpty(propertyMatcherExpr_)) {
      com.google.protobuf.GeneratedMessage.writeString(output, 2, propertyMatcherExpr_);
    }
    getUnknownFields().writeTo(output);
  }

  @java.lang.Override
  public int getSerializedSize() {
    int size = memoizedSize;
    if (size != -1) return size;

    size = 0;
    if (!com.google.protobuf.GeneratedMessage.isStringEmpty(labelMatcherExpr_)) {
      size += com.google.protobuf.GeneratedMessage.computeStringSize(1, labelMatcherExpr_);
    }
    if (!com.google.protobuf.GeneratedMessage.isStringEmpty(propertyMatcherExpr_)) {
      size += com.google.protobuf.GeneratedMessage.computeStringSize(2, propertyMatcherExpr_);
    }
    size += getUnknownFields().getSerializedSize();
    memoizedSize = size;
    return size;
  }

  @java.lang.Override
  public boolean equals(final java.lang.Object obj) {
    if (obj == this) {
      return true;
    }
    if (!(obj instanceof com.google.cloud.apptopology.v1.LabelPropertiesPattern)) {
      return super.equals(obj);
    }
    com.google.cloud.apptopology.v1.LabelPropertiesPattern other =
        (com.google.cloud.apptopology.v1.LabelPropertiesPattern) obj;

    if (!getLabelMatcherExpr().equals(other.getLabelMatcherExpr())) return false;
    if (!getPropertyMatcherExpr().equals(other.getPropertyMatcherExpr())) return false;
    if (!getUnknownFields().equals(other.getUnknownFields())) return false;
    return true;
  }

  @java.lang.Override
  public int hashCode() {
    if (memoizedHashCode != 0) {
      return memoizedHashCode;
    }
    int hash = 41;
    hash = (19 * hash) + getDescriptor().hashCode();
    hash = (37 * hash) + LABEL_MATCHER_EXPR_FIELD_NUMBER;
    hash = (53 * hash) + getLabelMatcherExpr().hashCode();
    hash = (37 * hash) + PROPERTY_MATCHER_EXPR_FIELD_NUMBER;
    hash = (53 * hash) + getPropertyMatcherExpr().hashCode();
    hash = (29 * hash) + getUnknownFields().hashCode();
    memoizedHashCode = hash;
    return hash;
  }

  public static com.google.cloud.apptopology.v1.LabelPropertiesPattern parseFrom(
      java.nio.ByteBuffer data) throws com.google.protobuf.InvalidProtocolBufferException {
    return PARSER.parseFrom(data);
  }

  public static com.google.cloud.apptopology.v1.LabelPropertiesPattern parseFrom(
      java.nio.ByteBuffer data, com.google.protobuf.ExtensionRegistryLite extensionRegistry)
      throws com.google.protobuf.InvalidProtocolBufferException {
    return PARSER.parseFrom(data, extensionRegistry);
  }

  public static com.google.cloud.apptopology.v1.LabelPropertiesPattern parseFrom(
      com.google.protobuf.ByteString data)
      throws com.google.protobuf.InvalidProtocolBufferException {
    return PARSER.parseFrom(data);
  }

  public static com.google.cloud.apptopology.v1.LabelPropertiesPattern parseFrom(
      com.google.protobuf.ByteString data,
      com.google.protobuf.ExtensionRegistryLite extensionRegistry)
      throws com.google.protobuf.InvalidProtocolBufferException {
    return PARSER.parseFrom(data, extensionRegistry);
  }

  public static com.google.cloud.apptopology.v1.LabelPropertiesPattern parseFrom(byte[] data)
      throws com.google.protobuf.InvalidProtocolBufferException {
    return PARSER.parseFrom(data);
  }

  public static com.google.cloud.apptopology.v1.LabelPropertiesPattern parseFrom(
      byte[] data, com.google.protobuf.ExtensionRegistryLite extensionRegistry)
      throws com.google.protobuf.InvalidProtocolBufferException {
    return PARSER.parseFrom(data, extensionRegistry);
  }

  public static com.google.cloud.apptopology.v1.LabelPropertiesPattern parseFrom(
      java.io.InputStream input) throws java.io.IOException {
    return com.google.protobuf.GeneratedMessage.parseWithIOException(PARSER, input);
  }

  public static com.google.cloud.apptopology.v1.LabelPropertiesPattern parseFrom(
      java.io.InputStream input, com.google.protobuf.ExtensionRegistryLite extensionRegistry)
      throws java.io.IOException {
    return com.google.protobuf.GeneratedMessage.parseWithIOException(
        PARSER, input, extensionRegistry);
  }

  public static com.google.cloud.apptopology.v1.LabelPropertiesPattern parseDelimitedFrom(
      java.io.InputStream input) throws java.io.IOException {
    return com.google.protobuf.GeneratedMessage.parseDelimitedWithIOException(PARSER, input);
  }

  public static com.google.cloud.apptopology.v1.LabelPropertiesPattern parseDelimitedFrom(
      java.io.InputStream input, com.google.protobuf.ExtensionRegistryLite extensionRegistry)
      throws java.io.IOException {
    return com.google.protobuf.GeneratedMessage.parseDelimitedWithIOException(
        PARSER, input, extensionRegistry);
  }

  public static com.google.cloud.apptopology.v1.LabelPropertiesPattern parseFrom(
      com.google.protobuf.CodedInputStream input) throws java.io.IOException {
    return com.google.protobuf.GeneratedMessage.parseWithIOException(PARSER, input);
  }

  public static com.google.cloud.apptopology.v1.LabelPropertiesPattern parseFrom(
      com.google.protobuf.CodedInputStream input,
      com.google.protobuf.ExtensionRegistryLite extensionRegistry)
      throws java.io.IOException {
    return com.google.protobuf.GeneratedMessage.parseWithIOException(
        PARSER, input, extensionRegistry);
  }

  @java.lang.Override
  public Builder newBuilderForType() {
    return newBuilder();
  }

  public static Builder newBuilder() {
    return DEFAULT_INSTANCE.toBuilder();
  }

  public static Builder newBuilder(
      com.google.cloud.apptopology.v1.LabelPropertiesPattern prototype) {
    return DEFAULT_INSTANCE.toBuilder().mergeFrom(prototype);
  }

  @java.lang.Override
  public Builder toBuilder() {
    return this == DEFAULT_INSTANCE ? new Builder() : new Builder().mergeFrom(this);
  }

  @java.lang.Override
  protected Builder newBuilderForType(com.google.protobuf.GeneratedMessage.BuilderParent parent) {
    Builder builder = new Builder(parent);
    return builder;
  }

  /**
   *
   *
   * <pre>
   * Matcher for label and property. The matcher accepts both exact match and
   * expression.
   * </pre>
   *
   * Protobuf type {@code google.cloud.apptopology.v1.LabelPropertiesPattern}
   */
  public static final class Builder extends com.google.protobuf.GeneratedMessage.Builder<Builder>
      implements
      // @@protoc_insertion_point(builder_implements:google.cloud.apptopology.v1.LabelPropertiesPattern)
      com.google.cloud.apptopology.v1.LabelPropertiesPatternOrBuilder {
    public static final com.google.protobuf.Descriptors.Descriptor getDescriptor() {
      return com.google.cloud.apptopology.v1.QueryProto
          .internal_static_google_cloud_apptopology_v1_LabelPropertiesPattern_descriptor;
    }

    @java.lang.Override
    protected com.google.protobuf.GeneratedMessage.FieldAccessorTable
        internalGetFieldAccessorTable() {
      return com.google.cloud.apptopology.v1.QueryProto
          .internal_static_google_cloud_apptopology_v1_LabelPropertiesPattern_fieldAccessorTable
          .ensureFieldAccessorsInitialized(
              com.google.cloud.apptopology.v1.LabelPropertiesPattern.class,
              com.google.cloud.apptopology.v1.LabelPropertiesPattern.Builder.class);
    }

    // Construct using com.google.cloud.apptopology.v1.LabelPropertiesPattern.newBuilder()
    private Builder() {}

    private Builder(com.google.protobuf.GeneratedMessage.BuilderParent parent) {
      super(parent);
    }

    @java.lang.Override
    public Builder clear() {
      super.clear();
      bitField0_ = 0;
      labelMatcherExpr_ = "";
      propertyMatcherExpr_ = "";
      return this;
    }

    @java.lang.Override
    public com.google.protobuf.Descriptors.Descriptor getDescriptorForType() {
      return com.google.cloud.apptopology.v1.QueryProto
          .internal_static_google_cloud_apptopology_v1_LabelPropertiesPattern_descriptor;
    }

    @java.lang.Override
    public com.google.cloud.apptopology.v1.LabelPropertiesPattern getDefaultInstanceForType() {
      return com.google.cloud.apptopology.v1.LabelPropertiesPattern.getDefaultInstance();
    }

    @java.lang.Override
    public com.google.cloud.apptopology.v1.LabelPropertiesPattern build() {
      com.google.cloud.apptopology.v1.LabelPropertiesPattern result = buildPartial();
      if (!result.isInitialized()) {
        throw newUninitializedMessageException(result);
      }
      return result;
    }

    @java.lang.Override
    public com.google.cloud.apptopology.v1.LabelPropertiesPattern buildPartial() {
      com.google.cloud.apptopology.v1.LabelPropertiesPattern result =
          new com.google.cloud.apptopology.v1.LabelPropertiesPattern(this);
      if (bitField0_ != 0) {
        buildPartial0(result);
      }
      onBuilt();
      return result;
    }

    private void buildPartial0(com.google.cloud.apptopology.v1.LabelPropertiesPattern result) {
      int from_bitField0_ = bitField0_;
      if (((from_bitField0_ & 0x00000001) != 0)) {
        result.labelMatcherExpr_ = labelMatcherExpr_;
      }
      if (((from_bitField0_ & 0x00000002) != 0)) {
        result.propertyMatcherExpr_ = propertyMatcherExpr_;
      }
    }

    @java.lang.Override
    public Builder mergeFrom(com.google.protobuf.Message other) {
      if (other instanceof com.google.cloud.apptopology.v1.LabelPropertiesPattern) {
        return mergeFrom((com.google.cloud.apptopology.v1.LabelPropertiesPattern) other);
      } else {
        super.mergeFrom(other);
        return this;
      }
    }

    public Builder mergeFrom(com.google.cloud.apptopology.v1.LabelPropertiesPattern other) {
      if (other == com.google.cloud.apptopology.v1.LabelPropertiesPattern.getDefaultInstance())
        return this;
      if (!other.getLabelMatcherExpr().isEmpty()) {
        labelMatcherExpr_ = other.labelMatcherExpr_;
        bitField0_ |= 0x00000001;
        onChanged();
      }
      if (!other.getPropertyMatcherExpr().isEmpty()) {
        propertyMatcherExpr_ = other.propertyMatcherExpr_;
        bitField0_ |= 0x00000002;
        onChanged();
      }
      this.mergeUnknownFields(other.getUnknownFields());
      onChanged();
      return this;
    }

    @java.lang.Override
    public final boolean isInitialized() {
      return true;
    }

    @java.lang.Override
    public Builder mergeFrom(
        com.google.protobuf.CodedInputStream input,
        com.google.protobuf.ExtensionRegistryLite extensionRegistry)
        throws java.io.IOException {
      if (extensionRegistry == null) {
        throw new java.lang.NullPointerException();
      }
      try {
        boolean done = false;
        while (!done) {
          int tag = input.readTag();
          switch (tag) {
            case 0:
              done = true;
              break;
            case 10:
              {
                labelMatcherExpr_ = input.readStringRequireUtf8();
                bitField0_ |= 0x00000001;
                break;
              } // case 10
            case 18:
              {
                propertyMatcherExpr_ = input.readStringRequireUtf8();
                bitField0_ |= 0x00000002;
                break;
              } // case 18
            default:
              {
                if (!super.parseUnknownField(input, extensionRegistry, tag)) {
                  done = true; // was an endgroup tag
                }
                break;
              } // default:
          } // switch (tag)
        } // while (!done)
      } catch (com.google.protobuf.InvalidProtocolBufferException e) {
        throw e.unwrapIOException();
      } finally {
        onChanged();
      } // finally
      return this;
    }

    private int bitField0_;

    private java.lang.Object labelMatcherExpr_ = "";

    /**
     *
     *
     * <pre>
     * Required. Matcher expression for labels. Allow OR / AND operators;
     * The syntax is defined by the following grammar:
     * - The label expression can use either `OR` or `AND` operator, but not a
     * combination of both.
     * Example: `AppHub/Application`, `AppHub/Workload OR AppHub/Service`
     * </pre>
     *
     * <code>string label_matcher_expr = 1 [(.google.api.field_behavior) = REQUIRED];</code>
     *
     * @return The labelMatcherExpr.
     */
    public java.lang.String getLabelMatcherExpr() {
      java.lang.Object ref = labelMatcherExpr_;
      if (!(ref instanceof java.lang.String)) {
        com.google.protobuf.ByteString bs = (com.google.protobuf.ByteString) ref;
        java.lang.String s = bs.toStringUtf8();
        labelMatcherExpr_ = s;
        return s;
      } else {
        return (java.lang.String) ref;
      }
    }

    /**
     *
     *
     * <pre>
     * Required. Matcher expression for labels. Allow OR / AND operators;
     * The syntax is defined by the following grammar:
     * - The label expression can use either `OR` or `AND` operator, but not a
     * combination of both.
     * Example: `AppHub/Application`, `AppHub/Workload OR AppHub/Service`
     * </pre>
     *
     * <code>string label_matcher_expr = 1 [(.google.api.field_behavior) = REQUIRED];</code>
     *
     * @return The bytes for labelMatcherExpr.
     */
    public com.google.protobuf.ByteString getLabelMatcherExprBytes() {
      java.lang.Object ref = labelMatcherExpr_;
      if (ref instanceof String) {
        com.google.protobuf.ByteString b =
            com.google.protobuf.ByteString.copyFromUtf8((java.lang.String) ref);
        labelMatcherExpr_ = b;
        return b;
      } else {
        return (com.google.protobuf.ByteString) ref;
      }
    }

    /**
     *
     *
     * <pre>
     * Required. Matcher expression for labels. Allow OR / AND operators;
     * The syntax is defined by the following grammar:
     * - The label expression can use either `OR` or `AND` operator, but not a
     * combination of both.
     * Example: `AppHub/Application`, `AppHub/Workload OR AppHub/Service`
     * </pre>
     *
     * <code>string label_matcher_expr = 1 [(.google.api.field_behavior) = REQUIRED];</code>
     *
     * @param value The labelMatcherExpr to set.
     * @return This builder for chaining.
     */
    public Builder setLabelMatcherExpr(java.lang.String value) {
      if (value == null) {
        throw new NullPointerException();
      }
      labelMatcherExpr_ = value;
      bitField0_ |= 0x00000001;
      onChanged();
      return this;
    }

    /**
     *
     *
     * <pre>
     * Required. Matcher expression for labels. Allow OR / AND operators;
     * The syntax is defined by the following grammar:
     * - The label expression can use either `OR` or `AND` operator, but not a
     * combination of both.
     * Example: `AppHub/Application`, `AppHub/Workload OR AppHub/Service`
     * </pre>
     *
     * <code>string label_matcher_expr = 1 [(.google.api.field_behavior) = REQUIRED];</code>
     *
     * @return This builder for chaining.
     */
    public Builder clearLabelMatcherExpr() {
      labelMatcherExpr_ = getDefaultInstance().getLabelMatcherExpr();
      bitField0_ = (bitField0_ & ~0x00000001);
      onChanged();
      return this;
    }

    /**
     *
     *
     * <pre>
     * Required. Matcher expression for labels. Allow OR / AND operators;
     * The syntax is defined by the following grammar:
     * - The label expression can use either `OR` or `AND` operator, but not a
     * combination of both.
     * Example: `AppHub/Application`, `AppHub/Workload OR AppHub/Service`
     * </pre>
     *
     * <code>string label_matcher_expr = 1 [(.google.api.field_behavior) = REQUIRED];</code>
     *
     * @param value The bytes for labelMatcherExpr to set.
     * @return This builder for chaining.
     */
    public Builder setLabelMatcherExprBytes(com.google.protobuf.ByteString value) {
      if (value == null) {
        throw new NullPointerException();
      }
      checkByteStringIsUtf8(value);
      labelMatcherExpr_ = value;
      bitField0_ |= 0x00000001;
      onChanged();
      return this;
    }

    private java.lang.Object propertyMatcherExpr_ = "";

    /**
     *
     *
     * <pre>
     * Optional. The syntax of the filter is defined by the following grammar:
     * https://google.aip.dev/160.
     *
     * Referencing properties of graph elements using traversal operator `.`.
     * Repeated properties cannot be traversed.
     * e.g. `e.NodeName`, where `e` is a node or edge alias.
     *
     * The following properties are supported for all node and edge types:
     *
     * - `Labels`: repeated string property. Can be used to combine conditions
     * between labels and properties.
     * e.g. `e.Labels : "label_1"` to match a node/edge with label
     * `label_1`.
     * - `NodeName/EdgeName`: string property. Support for filtering by node or
     * edge names.
     * e.g. `app.NodeName = "apphub.io/Application"`
     *
     * Other properties types can be found at
     * https://apptopology.googleapis.com/v1/GetSchema
     *
     * The following operators are supported on properties:
     * 1. Checking if a non-repeated property of a graph element is equal to a
     * scalar value using the equality operator `=`.
     * - LHS must be a node/edge property.
     * - RHS must be a scalar literal.
     * - Supported value types: string (enclosed in double quotes), int, double,
     * and boolean.
     * e.g. `e.project = "project_1"`
     *
     * 2. Checking if a non-repeated property of a graph element satisfies a
     * condition using the comparison operators `&gt;`, `&lt;`, `&gt;=`, `&lt;=`.
     * - LHS must be a node/edge property.
     * - RHS must be a scalar literal.
     * - Supported value types: string (enclosed in double quotes), int, double.
     * e.g. `e.NumericProperty &lt; 0.1`
     *
     * 3. Checking if a scalar value is present in a repeated property
     * HAS operator `:`.
     * - LHS must be a repeated property.
     * - RHS must be a scalar literal.
     * - Supported value types: string (enclosed in double quotes), int, double.
     * e.g. `e.Labels : "label_1"`, where `e.Labels` is a repeated string
     * property.
     *
     * 4. Checking if a property is present using HAS_PROPERTY operator `:`.
     * - LHS must be a node or edge alias.
     * - RHS must be a property name.
     * e.g. `e : Base/Location`, where `e` is a node or edge alias.
     *
     * 5. Combining multiple filters using logical operators : `AND`, and `OR`.
     * - Parentheses must be used to resolve ambiguity.
     * e.g. `e.Base/Location = "us-central1" AND e.project = "project_1"`
     *
     * 6. The following custom functions are supported:
     * - **EQUALS_ANY(arg1, arg2, arg3, ...)**
     * * Checks if `arg1` (a non-repeated property) equals any of the
     * subsequent scalar literal arguments.
     * * arg1: non-repeated property. Supported value types: string
     * (enclosed in double quotes), int, double.
     * * arg2, arg3, ...: The remaining arguments are literal values to
     * compare against. All the arguments must be of the same type.
     * * e.g. `EQUALS_ANY(e.project, "project_1", "project_2")`
     *
     * - **CONTAINS_ANY(arg1, arg2, arg3, ...)**
     * * Checks if a repeated property has any of the given scalar literals.
     * arg1: repeated_property. Supported element types: string
     * (enclosed in double quotes), int, double.
     * * arg2, arg3, ...: The remaining arguments are literal values to
     * compare against. All the arguments must be of the same type.
     * * e.g. `CONTAINS_ANY(e.folders, "folder_1", "folder_2")`
     *
     * - **CONTAINS_ANY_LESS_THAN(arg1, arg2)**
     * * Checks if a repeated property has any element less than the given
     * scalar literal.
     * * arg1: repeated_property. Supported element types are: int,
     * double.
     * * arg2: scalar literal
     * * e.g. `CONTAINS_ANY_LESS_THAN(e.NumericProperty, 0.1)`
     *
     * - **CONTAINS_ANY_GREATER_THAN(arg1, arg2)**
     * * Checks if a repeated property has any element greater than the given
     * scalar literal.
     * * arg1: repeated_property. Supported element types are: int,
     * double.
     * * arg2: scalar literal
     * * e.g. `CONTAINS_ANY_GREATER_THAN(e.NumericProperty, 0.1)`
     *
     * - **CONTAINS_ANY_LESS_OR_EQUAL(arg1, arg2)**
     * * Checks if a repeated property has any element less than or equal to
     * the given scalar literal.
     * * arg1: repeated_property. Supported element types are: int,
     * double.
     * * arg2: scalar literal
     * * e.g. `CONTAINS_ANY_LESS_OR_EQUAL(e.NumericProperty, 0.1)`
     *
     * - **CONTAINS_ANY_GREATER_OR_EQUAL(arg1, arg2)**
     * * Checks if a repeated property has any element greater than or equal
     * to the given scalar literal.
     * * arg1: repeated_property. Supported element types are: int,
     * double.
     * * arg2: scalar literal
     * * e.g. `CONTAINS_ANY_GREATER_OR_EQUAL(e.NumericProperty, 0.1)`
     *
     * NOTE:
     * - For Alert nodes, only the equality operator `=` is supported.
     * The expression cannot be a composite boolean expression (i.e., combining
     * multiple conditions with AND/OR is not supported; it must be simple).
     * e.g. `a.NodeName = "alert-1"` is supported, but
     * `a.NodeName = "alert-1" OR a.NodeName = "alert-2"` is not supported.
     * - For traffic edges, filters cannot be added.
     * </pre>
     *
     * <code>string property_matcher_expr = 2 [(.google.api.field_behavior) = OPTIONAL];</code>
     *
     * @return The propertyMatcherExpr.
     */
    public java.lang.String getPropertyMatcherExpr() {
      java.lang.Object ref = propertyMatcherExpr_;
      if (!(ref instanceof java.lang.String)) {
        com.google.protobuf.ByteString bs = (com.google.protobuf.ByteString) ref;
        java.lang.String s = bs.toStringUtf8();
        propertyMatcherExpr_ = s;
        return s;
      } else {
        return (java.lang.String) ref;
      }
    }

    /**
     *
     *
     * <pre>
     * Optional. The syntax of the filter is defined by the following grammar:
     * https://google.aip.dev/160.
     *
     * Referencing properties of graph elements using traversal operator `.`.
     * Repeated properties cannot be traversed.
     * e.g. `e.NodeName`, where `e` is a node or edge alias.
     *
     * The following properties are supported for all node and edge types:
     *
     * - `Labels`: repeated string property. Can be used to combine conditions
     * between labels and properties.
     * e.g. `e.Labels : "label_1"` to match a node/edge with label
     * `label_1`.
     * - `NodeName/EdgeName`: string property. Support for filtering by node or
     * edge names.
     * e.g. `app.NodeName = "apphub.io/Application"`
     *
     * Other properties types can be found at
     * https://apptopology.googleapis.com/v1/GetSchema
     *
     * The following operators are supported on properties:
     * 1. Checking if a non-repeated property of a graph element is equal to a
     * scalar value using the equality operator `=`.
     * - LHS must be a node/edge property.
     * - RHS must be a scalar literal.
     * - Supported value types: string (enclosed in double quotes), int, double,
     * and boolean.
     * e.g. `e.project = "project_1"`
     *
     * 2. Checking if a non-repeated property of a graph element satisfies a
     * condition using the comparison operators `&gt;`, `&lt;`, `&gt;=`, `&lt;=`.
     * - LHS must be a node/edge property.
     * - RHS must be a scalar literal.
     * - Supported value types: string (enclosed in double quotes), int, double.
     * e.g. `e.NumericProperty &lt; 0.1`
     *
     * 3. Checking if a scalar value is present in a repeated property
     * HAS operator `:`.
     * - LHS must be a repeated property.
     * - RHS must be a scalar literal.
     * - Supported value types: string (enclosed in double quotes), int, double.
     * e.g. `e.Labels : "label_1"`, where `e.Labels` is a repeated string
     * property.
     *
     * 4. Checking if a property is present using HAS_PROPERTY operator `:`.
     * - LHS must be a node or edge alias.
     * - RHS must be a property name.
     * e.g. `e : Base/Location`, where `e` is a node or edge alias.
     *
     * 5. Combining multiple filters using logical operators : `AND`, and `OR`.
     * - Parentheses must be used to resolve ambiguity.
     * e.g. `e.Base/Location = "us-central1" AND e.project = "project_1"`
     *
     * 6. The following custom functions are supported:
     * - **EQUALS_ANY(arg1, arg2, arg3, ...)**
     * * Checks if `arg1` (a non-repeated property) equals any of the
     * subsequent scalar literal arguments.
     * * arg1: non-repeated property. Supported value types: string
     * (enclosed in double quotes), int, double.
     * * arg2, arg3, ...: The remaining arguments are literal values to
     * compare against. All the arguments must be of the same type.
     * * e.g. `EQUALS_ANY(e.project, "project_1", "project_2")`
     *
     * - **CONTAINS_ANY(arg1, arg2, arg3, ...)**
     * * Checks if a repeated property has any of the given scalar literals.
     * arg1: repeated_property. Supported element types: string
     * (enclosed in double quotes), int, double.
     * * arg2, arg3, ...: The remaining arguments are literal values to
     * compare against. All the arguments must be of the same type.
     * * e.g. `CONTAINS_ANY(e.folders, "folder_1", "folder_2")`
     *
     * - **CONTAINS_ANY_LESS_THAN(arg1, arg2)**
     * * Checks if a repeated property has any element less than the given
     * scalar literal.
     * * arg1: repeated_property. Supported element types are: int,
     * double.
     * * arg2: scalar literal
     * * e.g. `CONTAINS_ANY_LESS_THAN(e.NumericProperty, 0.1)`
     *
     * - **CONTAINS_ANY_GREATER_THAN(arg1, arg2)**
     * * Checks if a repeated property has any element greater than the given
     * scalar literal.
     * * arg1: repeated_property. Supported element types are: int,
     * double.
     * * arg2: scalar literal
     * * e.g. `CONTAINS_ANY_GREATER_THAN(e.NumericProperty, 0.1)`
     *
     * - **CONTAINS_ANY_LESS_OR_EQUAL(arg1, arg2)**
     * * Checks if a repeated property has any element less than or equal to
     * the given scalar literal.
     * * arg1: repeated_property. Supported element types are: int,
     * double.
     * * arg2: scalar literal
     * * e.g. `CONTAINS_ANY_LESS_OR_EQUAL(e.NumericProperty, 0.1)`
     *
     * - **CONTAINS_ANY_GREATER_OR_EQUAL(arg1, arg2)**
     * * Checks if a repeated property has any element greater than or equal
     * to the given scalar literal.
     * * arg1: repeated_property. Supported element types are: int,
     * double.
     * * arg2: scalar literal
     * * e.g. `CONTAINS_ANY_GREATER_OR_EQUAL(e.NumericProperty, 0.1)`
     *
     * NOTE:
     * - For Alert nodes, only the equality operator `=` is supported.
     * The expression cannot be a composite boolean expression (i.e., combining
     * multiple conditions with AND/OR is not supported; it must be simple).
     * e.g. `a.NodeName = "alert-1"` is supported, but
     * `a.NodeName = "alert-1" OR a.NodeName = "alert-2"` is not supported.
     * - For traffic edges, filters cannot be added.
     * </pre>
     *
     * <code>string property_matcher_expr = 2 [(.google.api.field_behavior) = OPTIONAL];</code>
     *
     * @return The bytes for propertyMatcherExpr.
     */
    public com.google.protobuf.ByteString getPropertyMatcherExprBytes() {
      java.lang.Object ref = propertyMatcherExpr_;
      if (ref instanceof String) {
        com.google.protobuf.ByteString b =
            com.google.protobuf.ByteString.copyFromUtf8((java.lang.String) ref);
        propertyMatcherExpr_ = b;
        return b;
      } else {
        return (com.google.protobuf.ByteString) ref;
      }
    }

    /**
     *
     *
     * <pre>
     * Optional. The syntax of the filter is defined by the following grammar:
     * https://google.aip.dev/160.
     *
     * Referencing properties of graph elements using traversal operator `.`.
     * Repeated properties cannot be traversed.
     * e.g. `e.NodeName`, where `e` is a node or edge alias.
     *
     * The following properties are supported for all node and edge types:
     *
     * - `Labels`: repeated string property. Can be used to combine conditions
     * between labels and properties.
     * e.g. `e.Labels : "label_1"` to match a node/edge with label
     * `label_1`.
     * - `NodeName/EdgeName`: string property. Support for filtering by node or
     * edge names.
     * e.g. `app.NodeName = "apphub.io/Application"`
     *
     * Other properties types can be found at
     * https://apptopology.googleapis.com/v1/GetSchema
     *
     * The following operators are supported on properties:
     * 1. Checking if a non-repeated property of a graph element is equal to a
     * scalar value using the equality operator `=`.
     * - LHS must be a node/edge property.
     * - RHS must be a scalar literal.
     * - Supported value types: string (enclosed in double quotes), int, double,
     * and boolean.
     * e.g. `e.project = "project_1"`
     *
     * 2. Checking if a non-repeated property of a graph element satisfies a
     * condition using the comparison operators `&gt;`, `&lt;`, `&gt;=`, `&lt;=`.
     * - LHS must be a node/edge property.
     * - RHS must be a scalar literal.
     * - Supported value types: string (enclosed in double quotes), int, double.
     * e.g. `e.NumericProperty &lt; 0.1`
     *
     * 3. Checking if a scalar value is present in a repeated property
     * HAS operator `:`.
     * - LHS must be a repeated property.
     * - RHS must be a scalar literal.
     * - Supported value types: string (enclosed in double quotes), int, double.
     * e.g. `e.Labels : "label_1"`, where `e.Labels` is a repeated string
     * property.
     *
     * 4. Checking if a property is present using HAS_PROPERTY operator `:`.
     * - LHS must be a node or edge alias.
     * - RHS must be a property name.
     * e.g. `e : Base/Location`, where `e` is a node or edge alias.
     *
     * 5. Combining multiple filters using logical operators : `AND`, and `OR`.
     * - Parentheses must be used to resolve ambiguity.
     * e.g. `e.Base/Location = "us-central1" AND e.project = "project_1"`
     *
     * 6. The following custom functions are supported:
     * - **EQUALS_ANY(arg1, arg2, arg3, ...)**
     * * Checks if `arg1` (a non-repeated property) equals any of the
     * subsequent scalar literal arguments.
     * * arg1: non-repeated property. Supported value types: string
     * (enclosed in double quotes), int, double.
     * * arg2, arg3, ...: The remaining arguments are literal values to
     * compare against. All the arguments must be of the same type.
     * * e.g. `EQUALS_ANY(e.project, "project_1", "project_2")`
     *
     * - **CONTAINS_ANY(arg1, arg2, arg3, ...)**
     * * Checks if a repeated property has any of the given scalar literals.
     * arg1: repeated_property. Supported element types: string
     * (enclosed in double quotes), int, double.
     * * arg2, arg3, ...: The remaining arguments are literal values to
     * compare against. All the arguments must be of the same type.
     * * e.g. `CONTAINS_ANY(e.folders, "folder_1", "folder_2")`
     *
     * - **CONTAINS_ANY_LESS_THAN(arg1, arg2)**
     * * Checks if a repeated property has any element less than the given
     * scalar literal.
     * * arg1: repeated_property. Supported element types are: int,
     * double.
     * * arg2: scalar literal
     * * e.g. `CONTAINS_ANY_LESS_THAN(e.NumericProperty, 0.1)`
     *
     * - **CONTAINS_ANY_GREATER_THAN(arg1, arg2)**
     * * Checks if a repeated property has any element greater than the given
     * scalar literal.
     * * arg1: repeated_property. Supported element types are: int,
     * double.
     * * arg2: scalar literal
     * * e.g. `CONTAINS_ANY_GREATER_THAN(e.NumericProperty, 0.1)`
     *
     * - **CONTAINS_ANY_LESS_OR_EQUAL(arg1, arg2)**
     * * Checks if a repeated property has any element less than or equal to
     * the given scalar literal.
     * * arg1: repeated_property. Supported element types are: int,
     * double.
     * * arg2: scalar literal
     * * e.g. `CONTAINS_ANY_LESS_OR_EQUAL(e.NumericProperty, 0.1)`
     *
     * - **CONTAINS_ANY_GREATER_OR_EQUAL(arg1, arg2)**
     * * Checks if a repeated property has any element greater than or equal
     * to the given scalar literal.
     * * arg1: repeated_property. Supported element types are: int,
     * double.
     * * arg2: scalar literal
     * * e.g. `CONTAINS_ANY_GREATER_OR_EQUAL(e.NumericProperty, 0.1)`
     *
     * NOTE:
     * - For Alert nodes, only the equality operator `=` is supported.
     * The expression cannot be a composite boolean expression (i.e., combining
     * multiple conditions with AND/OR is not supported; it must be simple).
     * e.g. `a.NodeName = "alert-1"` is supported, but
     * `a.NodeName = "alert-1" OR a.NodeName = "alert-2"` is not supported.
     * - For traffic edges, filters cannot be added.
     * </pre>
     *
     * <code>string property_matcher_expr = 2 [(.google.api.field_behavior) = OPTIONAL];</code>
     *
     * @param value The propertyMatcherExpr to set.
     * @return This builder for chaining.
     */
    public Builder setPropertyMatcherExpr(java.lang.String value) {
      if (value == null) {
        throw new NullPointerException();
      }
      propertyMatcherExpr_ = value;
      bitField0_ |= 0x00000002;
      onChanged();
      return this;
    }

    /**
     *
     *
     * <pre>
     * Optional. The syntax of the filter is defined by the following grammar:
     * https://google.aip.dev/160.
     *
     * Referencing properties of graph elements using traversal operator `.`.
     * Repeated properties cannot be traversed.
     * e.g. `e.NodeName`, where `e` is a node or edge alias.
     *
     * The following properties are supported for all node and edge types:
     *
     * - `Labels`: repeated string property. Can be used to combine conditions
     * between labels and properties.
     * e.g. `e.Labels : "label_1"` to match a node/edge with label
     * `label_1`.
     * - `NodeName/EdgeName`: string property. Support for filtering by node or
     * edge names.
     * e.g. `app.NodeName = "apphub.io/Application"`
     *
     * Other properties types can be found at
     * https://apptopology.googleapis.com/v1/GetSchema
     *
     * The following operators are supported on properties:
     * 1. Checking if a non-repeated property of a graph element is equal to a
     * scalar value using the equality operator `=`.
     * - LHS must be a node/edge property.
     * - RHS must be a scalar literal.
     * - Supported value types: string (enclosed in double quotes), int, double,
     * and boolean.
     * e.g. `e.project = "project_1"`
     *
     * 2. Checking if a non-repeated property of a graph element satisfies a
     * condition using the comparison operators `&gt;`, `&lt;`, `&gt;=`, `&lt;=`.
     * - LHS must be a node/edge property.
     * - RHS must be a scalar literal.
     * - Supported value types: string (enclosed in double quotes), int, double.
     * e.g. `e.NumericProperty &lt; 0.1`
     *
     * 3. Checking if a scalar value is present in a repeated property
     * HAS operator `:`.
     * - LHS must be a repeated property.
     * - RHS must be a scalar literal.
     * - Supported value types: string (enclosed in double quotes), int, double.
     * e.g. `e.Labels : "label_1"`, where `e.Labels` is a repeated string
     * property.
     *
     * 4. Checking if a property is present using HAS_PROPERTY operator `:`.
     * - LHS must be a node or edge alias.
     * - RHS must be a property name.
     * e.g. `e : Base/Location`, where `e` is a node or edge alias.
     *
     * 5. Combining multiple filters using logical operators : `AND`, and `OR`.
     * - Parentheses must be used to resolve ambiguity.
     * e.g. `e.Base/Location = "us-central1" AND e.project = "project_1"`
     *
     * 6. The following custom functions are supported:
     * - **EQUALS_ANY(arg1, arg2, arg3, ...)**
     * * Checks if `arg1` (a non-repeated property) equals any of the
     * subsequent scalar literal arguments.
     * * arg1: non-repeated property. Supported value types: string
     * (enclosed in double quotes), int, double.
     * * arg2, arg3, ...: The remaining arguments are literal values to
     * compare against. All the arguments must be of the same type.
     * * e.g. `EQUALS_ANY(e.project, "project_1", "project_2")`
     *
     * - **CONTAINS_ANY(arg1, arg2, arg3, ...)**
     * * Checks if a repeated property has any of the given scalar literals.
     * arg1: repeated_property. Supported element types: string
     * (enclosed in double quotes), int, double.
     * * arg2, arg3, ...: The remaining arguments are literal values to
     * compare against. All the arguments must be of the same type.
     * * e.g. `CONTAINS_ANY(e.folders, "folder_1", "folder_2")`
     *
     * - **CONTAINS_ANY_LESS_THAN(arg1, arg2)**
     * * Checks if a repeated property has any element less than the given
     * scalar literal.
     * * arg1: repeated_property. Supported element types are: int,
     * double.
     * * arg2: scalar literal
     * * e.g. `CONTAINS_ANY_LESS_THAN(e.NumericProperty, 0.1)`
     *
     * - **CONTAINS_ANY_GREATER_THAN(arg1, arg2)**
     * * Checks if a repeated property has any element greater than the given
     * scalar literal.
     * * arg1: repeated_property. Supported element types are: int,
     * double.
     * * arg2: scalar literal
     * * e.g. `CONTAINS_ANY_GREATER_THAN(e.NumericProperty, 0.1)`
     *
     * - **CONTAINS_ANY_LESS_OR_EQUAL(arg1, arg2)**
     * * Checks if a repeated property has any element less than or equal to
     * the given scalar literal.
     * * arg1: repeated_property. Supported element types are: int,
     * double.
     * * arg2: scalar literal
     * * e.g. `CONTAINS_ANY_LESS_OR_EQUAL(e.NumericProperty, 0.1)`
     *
     * - **CONTAINS_ANY_GREATER_OR_EQUAL(arg1, arg2)**
     * * Checks if a repeated property has any element greater than or equal
     * to the given scalar literal.
     * * arg1: repeated_property. Supported element types are: int,
     * double.
     * * arg2: scalar literal
     * * e.g. `CONTAINS_ANY_GREATER_OR_EQUAL(e.NumericProperty, 0.1)`
     *
     * NOTE:
     * - For Alert nodes, only the equality operator `=` is supported.
     * The expression cannot be a composite boolean expression (i.e., combining
     * multiple conditions with AND/OR is not supported; it must be simple).
     * e.g. `a.NodeName = "alert-1"` is supported, but
     * `a.NodeName = "alert-1" OR a.NodeName = "alert-2"` is not supported.
     * - For traffic edges, filters cannot be added.
     * </pre>
     *
     * <code>string property_matcher_expr = 2 [(.google.api.field_behavior) = OPTIONAL];</code>
     *
     * @return This builder for chaining.
     */
    public Builder clearPropertyMatcherExpr() {
      propertyMatcherExpr_ = getDefaultInstance().getPropertyMatcherExpr();
      bitField0_ = (bitField0_ & ~0x00000002);
      onChanged();
      return this;
    }

    /**
     *
     *
     * <pre>
     * Optional. The syntax of the filter is defined by the following grammar:
     * https://google.aip.dev/160.
     *
     * Referencing properties of graph elements using traversal operator `.`.
     * Repeated properties cannot be traversed.
     * e.g. `e.NodeName`, where `e` is a node or edge alias.
     *
     * The following properties are supported for all node and edge types:
     *
     * - `Labels`: repeated string property. Can be used to combine conditions
     * between labels and properties.
     * e.g. `e.Labels : "label_1"` to match a node/edge with label
     * `label_1`.
     * - `NodeName/EdgeName`: string property. Support for filtering by node or
     * edge names.
     * e.g. `app.NodeName = "apphub.io/Application"`
     *
     * Other properties types can be found at
     * https://apptopology.googleapis.com/v1/GetSchema
     *
     * The following operators are supported on properties:
     * 1. Checking if a non-repeated property of a graph element is equal to a
     * scalar value using the equality operator `=`.
     * - LHS must be a node/edge property.
     * - RHS must be a scalar literal.
     * - Supported value types: string (enclosed in double quotes), int, double,
     * and boolean.
     * e.g. `e.project = "project_1"`
     *
     * 2. Checking if a non-repeated property of a graph element satisfies a
     * condition using the comparison operators `&gt;`, `&lt;`, `&gt;=`, `&lt;=`.
     * - LHS must be a node/edge property.
     * - RHS must be a scalar literal.
     * - Supported value types: string (enclosed in double quotes), int, double.
     * e.g. `e.NumericProperty &lt; 0.1`
     *
     * 3. Checking if a scalar value is present in a repeated property
     * HAS operator `:`.
     * - LHS must be a repeated property.
     * - RHS must be a scalar literal.
     * - Supported value types: string (enclosed in double quotes), int, double.
     * e.g. `e.Labels : "label_1"`, where `e.Labels` is a repeated string
     * property.
     *
     * 4. Checking if a property is present using HAS_PROPERTY operator `:`.
     * - LHS must be a node or edge alias.
     * - RHS must be a property name.
     * e.g. `e : Base/Location`, where `e` is a node or edge alias.
     *
     * 5. Combining multiple filters using logical operators : `AND`, and `OR`.
     * - Parentheses must be used to resolve ambiguity.
     * e.g. `e.Base/Location = "us-central1" AND e.project = "project_1"`
     *
     * 6. The following custom functions are supported:
     * - **EQUALS_ANY(arg1, arg2, arg3, ...)**
     * * Checks if `arg1` (a non-repeated property) equals any of the
     * subsequent scalar literal arguments.
     * * arg1: non-repeated property. Supported value types: string
     * (enclosed in double quotes), int, double.
     * * arg2, arg3, ...: The remaining arguments are literal values to
     * compare against. All the arguments must be of the same type.
     * * e.g. `EQUALS_ANY(e.project, "project_1", "project_2")`
     *
     * - **CONTAINS_ANY(arg1, arg2, arg3, ...)**
     * * Checks if a repeated property has any of the given scalar literals.
     * arg1: repeated_property. Supported element types: string
     * (enclosed in double quotes), int, double.
     * * arg2, arg3, ...: The remaining arguments are literal values to
     * compare against. All the arguments must be of the same type.
     * * e.g. `CONTAINS_ANY(e.folders, "folder_1", "folder_2")`
     *
     * - **CONTAINS_ANY_LESS_THAN(arg1, arg2)**
     * * Checks if a repeated property has any element less than the given
     * scalar literal.
     * * arg1: repeated_property. Supported element types are: int,
     * double.
     * * arg2: scalar literal
     * * e.g. `CONTAINS_ANY_LESS_THAN(e.NumericProperty, 0.1)`
     *
     * - **CONTAINS_ANY_GREATER_THAN(arg1, arg2)**
     * * Checks if a repeated property has any element greater than the given
     * scalar literal.
     * * arg1: repeated_property. Supported element types are: int,
     * double.
     * * arg2: scalar literal
     * * e.g. `CONTAINS_ANY_GREATER_THAN(e.NumericProperty, 0.1)`
     *
     * - **CONTAINS_ANY_LESS_OR_EQUAL(arg1, arg2)**
     * * Checks if a repeated property has any element less than or equal to
     * the given scalar literal.
     * * arg1: repeated_property. Supported element types are: int,
     * double.
     * * arg2: scalar literal
     * * e.g. `CONTAINS_ANY_LESS_OR_EQUAL(e.NumericProperty, 0.1)`
     *
     * - **CONTAINS_ANY_GREATER_OR_EQUAL(arg1, arg2)**
     * * Checks if a repeated property has any element greater than or equal
     * to the given scalar literal.
     * * arg1: repeated_property. Supported element types are: int,
     * double.
     * * arg2: scalar literal
     * * e.g. `CONTAINS_ANY_GREATER_OR_EQUAL(e.NumericProperty, 0.1)`
     *
     * NOTE:
     * - For Alert nodes, only the equality operator `=` is supported.
     * The expression cannot be a composite boolean expression (i.e., combining
     * multiple conditions with AND/OR is not supported; it must be simple).
     * e.g. `a.NodeName = "alert-1"` is supported, but
     * `a.NodeName = "alert-1" OR a.NodeName = "alert-2"` is not supported.
     * - For traffic edges, filters cannot be added.
     * </pre>
     *
     * <code>string property_matcher_expr = 2 [(.google.api.field_behavior) = OPTIONAL];</code>
     *
     * @param value The bytes for propertyMatcherExpr to set.
     * @return This builder for chaining.
     */
    public Builder setPropertyMatcherExprBytes(com.google.protobuf.ByteString value) {
      if (value == null) {
        throw new NullPointerException();
      }
      checkByteStringIsUtf8(value);
      propertyMatcherExpr_ = value;
      bitField0_ |= 0x00000002;
      onChanged();
      return this;
    }

    // @@protoc_insertion_point(builder_scope:google.cloud.apptopology.v1.LabelPropertiesPattern)
  }

  // @@protoc_insertion_point(class_scope:google.cloud.apptopology.v1.LabelPropertiesPattern)
  private static final com.google.cloud.apptopology.v1.LabelPropertiesPattern DEFAULT_INSTANCE;

  static {
    DEFAULT_INSTANCE = new com.google.cloud.apptopology.v1.LabelPropertiesPattern();
  }

  public static com.google.cloud.apptopology.v1.LabelPropertiesPattern getDefaultInstance() {
    return DEFAULT_INSTANCE;
  }

  private static final com.google.protobuf.Parser<LabelPropertiesPattern> PARSER =
      new com.google.protobuf.AbstractParser<LabelPropertiesPattern>() {
        @java.lang.Override
        public LabelPropertiesPattern parsePartialFrom(
            com.google.protobuf.CodedInputStream input,
            com.google.protobuf.ExtensionRegistryLite extensionRegistry)
            throws com.google.protobuf.InvalidProtocolBufferException {
          Builder builder = newBuilder();
          try {
            builder.mergeFrom(input, extensionRegistry);
          } catch (com.google.protobuf.InvalidProtocolBufferException e) {
            throw e.setUnfinishedMessage(builder.buildPartial());
          } catch (com.google.protobuf.UninitializedMessageException e) {
            throw e.asInvalidProtocolBufferException().setUnfinishedMessage(builder.buildPartial());
          } catch (java.io.IOException e) {
            throw new com.google.protobuf.InvalidProtocolBufferException(e)
                .setUnfinishedMessage(builder.buildPartial());
          }
          return builder.buildPartial();
        }
      };

  public static com.google.protobuf.Parser<LabelPropertiesPattern> parser() {
    return PARSER;
  }

  @java.lang.Override
  public com.google.protobuf.Parser<LabelPropertiesPattern> getParserForType() {
    return PARSER;
  }

  @java.lang.Override
  public com.google.cloud.apptopology.v1.LabelPropertiesPattern getDefaultInstanceForType() {
    return DEFAULT_INSTANCE;
  }
}
